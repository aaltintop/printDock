# SPIKE: Cart Transform `lineExpand` (PrintDock)

Use this template when validating `lineExpand` + signed `_pd_price_token` behavior on a dev store.

## Environment

- Shop domain:
- Theme:
- Cart Transform status (PrintDock onboarding):
- App proxy path prefix (`/apps/printdock`):

## Scenarios

| Scenario | Steps | Cart/checkout result | Notes |
|----------|--------|----------------------|-------|
| Single variant + upload fee | | | |
| Quantity change on cart page | | | |
| Selling plan line | | | (expect no transform) |
| Invalid / expired token | | | |
| Missing `__pd_now` cart attribute | | | |

## Raw observations

Paste response snippets, function logs, or storefront errors here.

## Conclusion

- **Ship / adjust / block:** 
- **Follow-ups:**
